---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Terminal from '../components/Terminal.astro';
import ServiceCard from '../components/ServiceCard.astro';
import ContactForm from '../components/ContactForm.astro';
import Footer from '../components/Footer.astro';

// Services data
const services = [
  {
    title: "System Administration",
    description: "Complete server management, user administration, and system monitoring to keep your infrastructure running smoothly.",
    icon: `<svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
      <path d="M12 2L2 7L12 12L22 7L12 2Z" stroke="currentColor" stroke-width="2" fill="none"/>
      <path d="M2 17L12 22L22 17" stroke="currentColor" stroke-width="2" fill="none"/>
      <path d="M2 12L12 17L22 12" stroke="currentColor" stroke-width="2" fill="none"/>
    </svg>`
  },
  {
    title: "Performance Optimization",
    description: "Fine-tune your Linux systems for maximum performance, efficiency, and resource utilization.",
    icon: `<svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
      <path d="M13 2L3 14H12L11 22L21 10H12L13 2Z" stroke="currentColor" stroke-width="2" fill="none"/>
    </svg>`
  },
  {
    title: "24/7 Support",
    description: "Round-the-clock technical support and emergency response to ensure your systems stay operational.",
    icon: `<svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
      <path d="M12 22C17.5228 22 22 17.5228 22 12C22 6.47715 17.5228 2 12 2C6.47715 2 2 6.47715 2 12C2 17.5228 6.47715 22 12 22Z" stroke="currentColor" stroke-width="2" fill="none"/>
      <path d="M12 6V12L16 14" stroke="currentColor" stroke-width="2" fill="none"/>
    </svg>`
  },
  {
    title: "Security Hardening",
    description: "Implement robust security measures, firewall configuration, and vulnerability assessments.",
    icon: `<svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
      <path d="M9 12L11 14L15 10" stroke="currentColor" stroke-width="2" fill="none"/>
      <path d="M21 12C21 16.9706 16.9706 21 12 21C7.02944 21 3 16.9706 3 12C3 7.02944 7.02944 3 12 3C16.9706 3 21 7.02944 21 12Z" stroke="currentColor" stroke-width="2" fill="none"/>
    </svg>`
  },
  {
    title: "Migration & Setup",
    description: "Seamless migration from other operating systems and complete Linux infrastructure setup.",
    icon: `<svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
      <path d="M4 19.5C4 18.1193 5.11929 17 6.5 17H20" stroke="currentColor" stroke-width="2" fill="none"/>
      <path d="M6.5 2H20V22H6.5C5.11929 22 4 20.8807 4 19.5V4.5C4 3.11929 5.11929 2 6.5 2Z" stroke="currentColor" stroke-width="2" fill="none"/>
    </svg>`
  },
  {
    title: "Training & Documentation",
    description: "Comprehensive training for your team and detailed documentation for ongoing maintenance.",
    icon: `<svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
      <path d="M12 2L2 7L12 12L22 7L12 2Z" stroke="currentColor" stroke-width="2" fill="none"/>
      <path d="M2 17L12 22L22 17" stroke="currentColor" stroke-width="2" fill="none"/>
      <path d="M2 12L12 17L22 12" stroke="currentColor" stroke-width="2" fill="none"/>
    </svg>`
  }
];
---

<Layout title="Sakkenix Cloud - Linux Consulting Services">
  <Header />
  
  <main>
    <!-- Hero Section -->
    <section class="hero">
      <div class="container">
        <div class="hero-content">
          <h1 class="hero-title">
            Expert Linux Consulting
            <span class="gradient-text">for Modern Businesses</span>
          </h1>
          <p class="hero-description">
            Professional Linux system administration, optimization, and consulting services. 
            From server setup to performance tuning, we help businesses leverage the power of open-source technology.
          </p>
          <div class="hero-actions">
            <a href="#contact" class="btn btn-primary">Get Started</a>
            <a href="#services" class="btn btn-secondary">Learn More</a>
          </div>
        </div>
        <div class="hero-visual">
          <Terminal />
        </div>
      </div>
    </section>

    <!-- Services Section -->
    <section id="services" class="services">
      <div class="container">
        <div class="section-header">
          <h2>Our Services</h2>
          <p>Comprehensive Linux solutions tailored to your business needs</p>
        </div>
        <div class="services-grid">
          {services.map(service => (
            <ServiceCard 
              title={service.title}
              description={service.description}
              icon={service.icon}
            />
          ))}
        </div>
      </div>
    </section>

    <!-- About Section -->
    <section id="about" class="about">
      <div class="container">
        <div class="about-content">
          <div class="about-text">
            <h2>Why Choose Sakkenix Cloud?</h2>
            <p>With years of experience in Linux system administration and a deep understanding of enterprise environments, we provide reliable, scalable solutions that drive your business forward.</p>
            <div class="features">
              <div class="feature">
                <div class="feature-icon">✓</div>
                <div class="feature-text">
                  <h4>Expert Knowledge</h4>
                  <p>Certified Linux professionals with extensive experience</p>
                </div>
              </div>
              <div class="feature">
                <div class="feature-icon">✓</div>
                <div class="feature-text">
                  <h4>Proven Track Record</h4>
                  <p>Successfully managed systems for businesses of all sizes</p>
                </div>
              </div>
              <div class="feature">
                <div class="feature-icon">✓</div>
                <div class="feature-text">
                  <h4>Cost Effective</h4>
                  <p>Open-source solutions that reduce licensing costs</p>
                </div>
              </div>
              <div class="feature">
                <div class="feature-icon">✓</div>
                <div class="feature-text">
                  <h4>Reliable Support</h4>
                  <p>24/7 monitoring and rapid response times</p>
                </div>
              </div>
            </div>
          </div>
          <div class="about-stats">
            <div class="stat">
              <div class="stat-number">99.9%</div>
              <div class="stat-label">Uptime Guarantee</div>
            </div>
            <div class="stat">
              <div class="stat-number">24/7</div>
              <div class="stat-label">Support Available</div>
            </div>
            <div class="stat">
              <div class="stat-number">15min</div>
              <div class="stat-label">Response Time</div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Contact Section -->
    <section id="contact" class="contact">
      <div class="container">
        <div class="section-header">
          <h2>Ready to Get Started?</h2>
          <p>Let's discuss how we can help optimize your Linux infrastructure</p>
        </div>
        <div class="contact-content">
          <div class="contact-info">
            <h3>Get in Touch</h3>
            <p>Contact us to schedule a consultation or discuss your specific needs.</p>
            <div class="contact-methods">
              <div class="contact-method">
                <div class="contact-icon">📧</div>
                <div>
                  <h4>Email</h4>
                  <p>hello@sakkenix.cloud</p>
                </div>
              </div>
              <div class="contact-method">
                <div class="contact-icon">💬</div>
                <div>
                  <h4>Consultation</h4>
                  <p>Free initial assessment</p>
                </div>
              </div>
            </div>
          </div>
          <ContactForm />
        </div>
      </div>
    </section>
  </main>

  <Footer />
</Layout>

<style>
  /* Hero Section */
  .hero {
    padding: calc(80px + var(--spacing-24)) 0 var(--spacing-24);
    background: linear-gradient(135deg, var(--bg-secondary) 0%, var(--bg-primary) 100%);
  }

  .hero .container {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--spacing-16);
    align-items: center;
  }

  .hero-title {
    font-size: var(--font-size-5xl);
    font-weight: 700;
    line-height: 1.1;
    margin-bottom: var(--spacing-6);
  }

  .gradient-text {
    background: linear-gradient(135deg, var(--primary-color), var(--accent-color));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-description {
    font-size: var(--font-size-lg);
    color: var(--text-secondary);
    margin-bottom: var(--spacing-8);
    line-height: 1.7;
  }

  .hero-actions {
    display: flex;
    gap: var(--spacing-4);
    flex-wrap: wrap;
  }

  /* Sections */
  .section-header {
    text-align: center;
    margin-bottom: var(--spacing-16);
  }

  .section-header h2 {
    margin-bottom: var(--spacing-4);
  }

  .section-header p {
    font-size: var(--font-size-lg);
    max-width: 600px;
    margin: 0 auto;
  }

  /* Services Section */
  .services {
    padding: var(--spacing-24) 0;
    background-color: var(--bg-secondary);
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: var(--spacing-8);
  }

  /* About Section */
  .about {
    padding: var(--spacing-24) 0;
  }

  .about-content {
    display: grid;
    grid-template-columns: 2fr 1fr;
    gap: var(--spacing-16);
    align-items: center;
  }

  .about-text h2 {
    margin-bottom: var(--spacing-6);
  }

  .about-text > p {
    font-size: var(--font-size-lg);
    margin-bottom: var(--spacing-8);
  }

  .features {
    display: grid;
    gap: var(--spacing-6);
  }

  .feature {
    display: flex;
    gap: var(--spacing-4);
    align-items: flex-start;
  }

  .feature-icon {
    width: 24px;
    height: 24px;
    background-color: var(--success-color);
    color: white;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: var(--font-size-sm);
    font-weight: bold;
    flex-shrink: 0;
    margin-top: 2px;
  }

  .feature-text h4 {
    margin-bottom: var(--spacing-1);
    color: var(--text-primary);
  }

  .feature-text p {
    color: var(--text-secondary);
    margin-bottom: 0;
  }

  .about-stats {
    display: grid;
    gap: var(--spacing-8);
  }

  .stat {
    text-align: center;
    padding: var(--spacing-6);
    background-color: var(--bg-secondary);
    border-radius: var(--border-radius-lg);
  }

  .stat-number {
    font-size: var(--font-size-4xl);
    font-weight: 700;
    color: var(--primary-color);
    margin-bottom: var(--spacing-2);
  }

  .stat-label {
    color: var(--text-secondary);
    font-weight: 500;
  }

  /* Contact Section */
  .contact {
    padding: var(--spacing-24) 0;
    background-color: var(--bg-secondary);
  }

  .contact-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--spacing-16);
    max-width: 1000px;
    margin: 0 auto;
  }

  .contact-info h3 {
    margin-bottom: var(--spacing-4);
  }

  .contact-info > p {
    margin-bottom: var(--spacing-8);
  }

  .contact-methods {
    display: grid;
    gap: var(--spacing-6);
  }

  .contact-method {
    display: flex;
    gap: var(--spacing-4);
    align-items: center;
  }

  .contact-icon {
    width: 48px;
    height: 48px;
    background-color: var(--primary-color);
    border-radius: var(--border-radius);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: var(--font-size-xl);
    color: white;
  }

  .contact-method h4 {
    margin-bottom: var(--spacing-1);
    color: var(--text-primary);
  }

  .contact-method p {
    color: var(--text-secondary);
    margin-bottom: 0;
  }

  /* Responsive Design */
  @media (max-width: 768px) {
    .hero .container {
      grid-template-columns: 1fr;
      text-align: center;
    }
    
    .hero-title {
      font-size: var(--font-size-4xl);
    }
    
    .hero-actions {
      justify-content: center;
    }
    
    .services-grid {
      grid-template-columns: 1fr;
    }
    
    .about-content {
      grid-template-columns: 1fr;
      text-align: center;
    }
    
    .contact-content {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 480px) {
    .hero-title {
      font-size: var(--font-size-3xl);
    }
    
    .hero-actions {
      flex-direction: column;
      align-items: center;
    }
  }
</style>

<script>
  // Smooth scrolling for anchor links
  document.addEventListener('DOMContentLoaded', function() {
    const anchorLinks = document.querySelectorAll('a[href^="#"]');
    anchorLinks.forEach(link => {
      link.addEventListener('click', function(e) {
        e.preventDefault();
        const targetId = this.getAttribute('href');
        const targetElement = document.querySelector(targetId);
        
        if (targetElement) {
          const headerHeight = document.querySelector('.header').offsetHeight;
          const targetPosition = targetElement.offsetTop - headerHeight - 20;
          
          window.scrollTo({
            top: targetPosition,
            behavior: 'smooth'
          });
        }
      });
    });
  });
</script> 